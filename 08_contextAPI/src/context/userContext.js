import React from "react";

const UserContext = React.createContext()

export default UserContext

// step 1 - use createCOntext method from react to create a context store it in a variable 
// and export edefault 
//Each context is a PROVIDER as it proides youa variable so it will be used as wrapper 
//so step 2 - create a provider\

// go to userContextProvider.jsx